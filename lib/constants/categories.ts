export const DEFAULT_CATEGORIES = {
	INFORMATION: "Information",
	TEXT_CHANNELS: "Text Channels",
	VOICE_CHANNELS: "Voice Channels",
} as const;

export const DEFAULT_CATEGORY_POSITIONS = {
	INFORMATION: 0,
	TEXT_CHANNELS: 1,
	VOICE_CHANNELS: 2,
} as const;
